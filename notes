BLABBER
Domain
    DataStore - InMemory (cc)
    -IDataStore
    bool create(IDatum datum)
    IDatum read(ISearchTemplate)
    bool update()
    bool delete(system id)
    users (username, contact info)
    searchUsers
    Blabs (text, timestamp, author)
    searchBlabs

class User {
    private String firstName; // + getter and setter
    private String lastName; // + getter and setter
    private String sysId; // + getter, generated by system UID generator
    private String email; // + getter and setter, is username
}

class Blab {
    private String text;
    private dateTime timeStamp;
    // object reference to user
}